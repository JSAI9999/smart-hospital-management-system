server:
  port: 8080

spring:
  application:
    name: healthcare-backend

  datasource:
    url: jdbc:mysql://localhost:3306/healthcare_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true
